- reset search if new character loaded or failed to load
- modify exact match somehow
- write help for search
- pearl of wisdom color, shrines color (use rendering from QTblEditor)
- try fixing disenchant
- compress and maybe encrypt txt data
-----
- move recent files to separate sub-menu
- show experince bar and completion of current level in %
- completed skill/stat quests
- export item to bbcode/html
- link for skillpoint planner
- generate some bbcode/html report about character and/or plugy stashes contents
- add a check for avoid >= 100%
- uncomment 'like duping items'
- d2s file association
- maybe win7 taskbar and mac dock menu support to open recent files
-----
- move most functionality from splitter to tableview
- search and fix all 'TODO:' in code
- remove all qDebug()
- copy files to destdir from .pro:
1.
linux-g++{
    #...
    EXTRA_BINFILES += \
        $${THIRDPARTY_PATH}/gstreamer-0.10/linux/plugins/libgstrtp.so \
        $${THIRDPARTY_PATH}/gstreamer-0.10/linux/plugins/libgstvideo4linux2.so
    for(FILE,EXTRA_BINFILES) {
        QMAKE_POST_LINK += $$quote(cp $${FILE} $${DESTDIR}$$escape_expand(\n\t))
    }
}

win32 {
    #...
    EXTRA_BINFILES += \
        $${THIRDPARTY_PATH}/glib-2.0/win32/bin/libglib-2.0.dll \
        $${THIRDPARTY_PATH}/glib-2.0/win32/bin/libgmodule-2.0.dll
    EXTRA_BINFILES_WIN = $${EXTRA_BINFILES}
    EXTRA_BINFILES_WIN ~= s,/,\\,g
    DESTDIR_WIN = $${DESTDIR}
    DESTDIR_WIN ~= s,/,\\,g
    for(FILE,EXTRA_BINFILES_WIN) {
        QMAKE_POST_LINK +=$$quote(cmd /c copy /y $${FILE} $${DESTDIR_WIN}$$escape_expand(\n\t))
    }
}

2. INSTALLS
images.path    = $${DESTDIR}/images
images.files   += images/splashscreen.png
images.files   += images/logo.png
INSTALLS       += images

write in changelog:
- correct text color on the class charm :P

v0.3:
- affix rlvl support
- drag & drop
- merc items
- create plugy stashes
- nice display of property 'Adds x-y ... damage'
- show UMO properties and color
- try unMO UMOs
- calculate correct str/dex bonus based on character stats
